{
  "name": "ai-studio-ghrab",
  "version": "0.16.0",
  "private": true,
  "description": "Profesionální PWA rozcestník školních AI nástrojů s filmovým desktopovým portálem, projekčním režimem, řízeným přístupem a diagnostikou",
  "type": "module",
  "scripts": {
    "sync": "node scripts/sync-registry.mjs",
    "sync:offline": "node scripts/sync-registry.mjs --offline",
    "build": "node scripts/build.mjs",
    "test": "node scripts/test.mjs",
    "check": "node scripts/test.mjs"
  },
  "engines": {
    "node": ">=18"
  }
}
