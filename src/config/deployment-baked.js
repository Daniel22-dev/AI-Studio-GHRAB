// Production builds replace this placeholder with a validated deployment
// profile. Keeping the source value empty preserves local source previews
// while ensuring a missing production profile cannot silently enable features.
export const BAKED_DEPLOYMENT_CONFIG = null;
